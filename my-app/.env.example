# Copy to .env.local for local development.
# On Vercel, set VITE_BACKEND_URL as a project environment variable instead,
# pointing at your deployed Render backend (e.g. https://medilens-backend.onrender.com).

VITE_BACKEND_URL=http://localhost:8000
