# Setup Guide

This file described the original local-only architecture (Flask + Ollama +
Node.js gateway). MediLens has since moved to a single FastAPI backend
(Gemini-powered) and a Vite/React frontend, deployable to Render + Vercel.

See [README.md](README.md) for current setup and deployment instructions.
